name: CI

# Runs the Go test suite with coverage on every push and pull request. On pushes
# to main it also regenerates the coverage badge (.github/badges/coverage.svg)
# and commits it back to main — the README references it by relative path, so it
# renders against whichever branch is being viewed. The commit carries [skip ci]
# so it does not re-trigger this workflow. Integration tests (the //go:build
# integration set, which need a Docker daemon and a real Claude binary) are
# excluded here.
#
# The end-to-end workflow test (the //go:build e2e set) runs in its own `e2e`
# job, kept separate from the fast unit suite: it drives the auth UI through a
# real headless Chrome via WebDriver, simulating the Docker and Anthropic layers.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Vet
        run: go vet ./...

      - name: Test with coverage
        run: go test -covermode=atomic -coverprofile=coverage.out ./...

      - name: Coverage summary
        run: go tool cover -func=coverage.out | tail -1

      - name: Publish coverage badge
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          set -euo pipefail
          pct=$(go tool cover -func=coverage.out | awk '/^total:/ {gsub(/%/,"",$3); print $3}')
          rounded=$(printf '%.0f' "$pct")
          color=red
          if   [ "$rounded" -ge 80 ]; then color=brightgreen
          elif [ "$rounded" -ge 60 ]; then color=yellow
          elif [ "$rounded" -ge 40 ]; then color=orange
          fi
          mkdir -p .github/badges
          curl -fsSL "https://img.shields.io/badge/coverage-${pct}%25-${color}" -o .github/badges/coverage.svg

          if git diff --quiet -- .github/badges/coverage.svg; then
            echo "coverage badge unchanged ($pct%)"
            exit 0
          fi
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/badges/coverage.svg
          git commit -m "ci: update coverage badge (${pct}%) [skip ci]"
          # Rebase onto any commits that landed since checkout, then push to main.
          git pull --rebase --autostash origin main
          git push origin HEAD:main

  e2e:
    name: E2E (WebDriver)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # On pull requests, check out the PR's head branch itself (not the
          # detached merge ref) so the refreshed screenshots can be committed
          # back onto it. github.head_ref is empty on push events, where checkout
          # falls back to the pushed ref.
          ref: ${{ github.head_ref }}

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Chrome and ChromeDriver
        id: setup-chrome
        uses: browser-actions/setup-chrome@v1
        with:
          install-chromedriver: true

      - name: Pin and verify Chrome and ChromeDriver for the test
        # The runner image preinstalls its own chromedriver (via $CHROMEWEBDRIVER);
        # override both so the test uses the matched pair this action installed.
        #
        # Prefer the action's outputs, but fall back to $PATH (the action also
        # installs both there) so an empty/renamed output does not silently leave
        # CHROMEWEBDRIVER as "." — which made findChromeDriver() fail, the e2e
        # test t.Skip, and the screenshot/README refresh never happen while CI
        # stayed green. Fail fast with the resolved paths if either is missing.
        run: |
          set -euo pipefail
          chrome='${{ steps.setup-chrome.outputs.chrome-path }}'
          driver='${{ steps.setup-chrome.outputs.chromedriver-path }}'
          [ -n "$chrome" ] || chrome="$(command -v google-chrome || command -v chrome || true)"
          [ -n "$driver" ] || driver="$(command -v chromedriver || true)"
          if [ -z "$chrome" ] || [ ! -x "$chrome" ]; then
            echo "::error::Chrome not found (chrome-path='${{ steps.setup-chrome.outputs.chrome-path }}', resolved='$chrome')"
            exit 1
          fi
          if [ -z "$driver" ] || [ ! -x "$driver" ]; then
            echo "::error::ChromeDriver not found (chromedriver-path='${{ steps.setup-chrome.outputs.chromedriver-path }}', resolved='$driver')"
            exit 1
          fi
          echo "CHROME_BIN=$chrome" >> "$GITHUB_ENV"
          echo "CHROMEWEBDRIVER=$(dirname "$driver")" >> "$GITHUB_ENV"
          "$chrome" --version
          "$driver" --version

      - name: Run end-to-end workflow test
        run: make test-e2e
        env:
          # The test saves screenshots of the auth page here when this is set.
          LLMBOX_E2E_SCREENSHOT_DIR: .github/screenshots

      - name: Upload auth-page screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: auth-screenshots
          path: .github/screenshots/*.png
          if-no-files-found: ignore

      - name: Commit refreshed screenshots to the PR branch
        # Capture happens in the e2e test above; here we commit the refreshed
        # screenshots back onto the PR branch so they land in the PR diff and get
        # reviewed alongside the change that altered the UI — rather than being
        # committed straight to main after the fact.
        #
        # Same-repo PRs only: fork PRs get a read-only GITHUB_TOKEN and cannot
        # push, so we skip the commit for them (the e2e test still runs and gates
        # the PR). [skip ci] on the bot's own commit avoids re-triggering CI.
        if: >-
          github.event_name == 'pull_request' &&
          github.event.pull_request.head.repo.full_name == github.repository
        run: |
          set -euo pipefail
          if ! git status --porcelain -- .github/screenshots | grep -q .; then
            echo "auth-page screenshots unchanged"
            exit 0
          fi
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/screenshots/*.png
          git commit -m "ci: refresh auth-page screenshots [skip ci]"
          # Rebase onto anything pushed to the PR branch since checkout, then push.
          git pull --rebase --autostash origin "${{ github.head_ref }}"
          git push origin "HEAD:${{ github.head_ref }}"
