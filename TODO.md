# TODO

* Log all commands run in the container in a way that Claude cannot prevent the recording in any way.
* Authenticate MCP clients per-user so box *creation* is no longer open, and bind a box to the specific initiator (owner-email binding) instead of any allow-listed user.
* Paginate container listing.