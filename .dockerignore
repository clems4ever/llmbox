# Keep the build context small and avoid leaking local secrets into images.
.git
.github
*.md
Dockerfile.*
docker-compose.yml
.env
.claude-config.seed.json
/llmbox-mcp

# ...but the box skills (SKILL.md files) must reach the claude-remote build.
!box-skills
